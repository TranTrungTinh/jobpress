export enum APP_ENV {
  APP_PORT = 'APP_PORT',
  DB_CONNECT = 'DB_CONNECT'
}

export const APP_CONFIG = {
  FILE_MAX_SIZE_UPLOAD: 1024 * 1024 * 5, // 5MB
  FILE_ALLOWS: ['jpg', 'jpeg', 'png', 'gif', 'pdf', 'doc', 'docx']
}

